**Proteger datos críticos para que nadie los cambie por error una vez creados (como el DNI o el número de expediente).**

Crea una interfaz `Expediente` donde el `id` y el `numeroExpediente` sean `readonly`. Intenta cambiar el valor después de crear el objeto y observa cómo TS te detiene.
