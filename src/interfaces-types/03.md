Reutilizar estructuras. Si todos tus formularios tienen un `id` y `fecha`, no los repitas; herédalos.

- **Ejercicio:** Crea una interfaz base `Auditoria` (con `creadoPor` y `fecha`). Luego, crea una interfaz `SolicitudCatastro` que **extienda** de `Auditoria` y añada los campos específicos del trámite.
