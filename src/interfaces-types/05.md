**Concepto:** Aplicar todo lo anterior para definir "la verdad" de tu base de datos o estado global.

- **Ejercicio:** Refactoriza tu `AppState` y tu `FormRow`. Define interfaces claras para `Zonificacion`, `UsoSuelo` y `Propietario`. Este es el archivo donde "pones orden" a todo lo que hemos hablado.
