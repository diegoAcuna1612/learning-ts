**Aprender a modelar datos complejos donde no siempre toda la información está disponible (como una observación que puede o no existir)**.

Ej: Define una interfaz `Predio` que tenga ubicación (calle, número) y una propiedad opcional `datosCatastrales` que sea otro objeto con `manzana` y `lote`.
